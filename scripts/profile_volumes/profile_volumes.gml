/// @description profile_volumes()
/// @function profile_volumes

audio_group_set_gain(audiogroup_default, global.setting_music / 10, 0)
audio_group_set_gain(audiogroup_sfx, global.setting_sfx / 10, 0)

return true