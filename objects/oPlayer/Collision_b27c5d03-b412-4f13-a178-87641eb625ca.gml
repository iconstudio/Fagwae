/// @description Getting a score

score += 100
if ++global.pscore mod 10 == 0 {
	//show_flashstring(x, y, "+" + string(floor(global.pscore / 10)), 3)
	//sinBassSamplePlay(global.SounDresource[13])
} else {
	//show_flashstring(x, y, "+" + number_format(global.pscore / 10), 2)
}

with other
	instance_destroy()
